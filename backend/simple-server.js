const express = require("express"); const app = express(); app.use(express.json()); app.get("/api/health", (req, res) => res.json({status: "ok", message: "AI Social Backend is running", timestamp: new Date().toISOString()})); app.get("/api/status", (req, res) => res.json({status: "ready", version: "1.0.0"})); app.listen(3001, "0.0.0.0", () => console.log("Simple backend running on port 3001"));
